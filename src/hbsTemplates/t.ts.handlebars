import request from '../util/request';
import { {{#importList}}{{this}}, {{/importList}} } from './type';
/**
 * {{desc}}
 */
export class {{className}} {
    {{#fnList}}
    /**
     * {{functonDesc}}
     */
    {{functonName}}(data: {
        {{#parameterList}}
        {{parameterName}}{{#unless required}}?{{/unless}}: {{parameterType}}, {{#if parameterDesc}}// {{/if}}{{parameterDesc}}
        {{/parameterList}}
    }): Promise<{{responseType}}> {
        return request(`{{url}}`, {
            method: '{{method}}',
        }, data);
    }
    {{/fnList}}
}
